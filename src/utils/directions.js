// directions util: input a screen coordinate outside of the screen, 
// get the nearest point at the edge of the screen

export const getDirection = ( farPoint ) => {
    return new Vector2({ x: 1, y: 1 });
}